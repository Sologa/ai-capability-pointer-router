interface:
  display_name: "AI Capability Pointer Router"
  short_description: "Routes AI capability repo pointers and documents a reusable repo taxonomy template."
  default_prompt: "Use $ai-capability-pointer-router to choose the right repo pointer before reading source files."
policy:
  allow_implicit_invocation: false
